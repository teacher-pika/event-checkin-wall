window.APP_CONFIG = {
    // --- 除錯與開發設定 ---

    // 是否在左上角顯示 "節點數：70" 的除錯資訊
    // 1: 顯示 | 0: 隱藏
    showNodeCount: 0,

    // 當滑鼠 hover 到空白節點上時，是否顯示節點編號 (例如：D01, E05)
    // 1: 顯示 | 0: 隱藏
    showHoverDebugNumber: 1,

    // 是否顯示「一鍵報到全部 (測試用)」按鈕
    // 1: 顯示 | 0: 隱藏
    showTestCheckinButton: 0,


    // --- 顯示設定 ---

    // 是否在報到人數統計中顯示總人數 (例如：顯示 "1 / 10" 或只顯示 "1")
    // 1: 顯示 (1 / 10) | 0: 隱藏 (1)
    showTotalCount: 0,

    // --- 壓力測試設定 ---

    // 是否啟用模擬使用者進行壓力測試。設為 0 則關閉。
    // 若設為 200，且 data.csv 中人數不足 200，系統將自動生成假資料補齊至 200 人。
    useMockUserCount: 0,
};
